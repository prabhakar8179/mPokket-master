<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ibrickedlabs.mpokket">

    <application
        android:allowBackup="true"
        android:icon="@drawable/mpokket"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Activities.PhoneAuthActivity" />
        <activity
            android:name=".Activities.EmailLogin"
            android:screenOrientation="portrait" />
        <activity android:name=".Activities.CreateUser" />
        <activity
            android:name=".Activities.RequestMoney"
            android:theme="@style/Toolbartheme" />
        <activity android:name=".Activities.HistoryActivity" />
        <activity android:name=".Activities.AccountDetails" />
        <activity android:name=".Activities.UploadActivity" />
        <activity android:name=".Activities.MyAccount" />
        <activity android:name=".Activities.AdminActivity" />
        <activity android:name=".Activities.AdminLogoutActivity"></activity>
    </application>

</manifest>